% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{cis_read}
\alias{cis_read}
\title{Read the CIS longitudinal dataset}
\usage{
cis_read(
  fecha_min = NULL,
  fecha_max = NULL,
  estudios = NULL,
  cols = NULL,
  keep_core_cols = TRUE,
  collect = TRUE
)
}
\arguments{
\item{fecha_min}{Minimum survey date, included. \code{NULL} means no lower bound.}

\item{fecha_max}{Maximum survey date, included. \code{NULL} means no upper bound.}

\item{estudios}{Optional vector of study codes.}

\item{cols}{Optional character vector of columns to select.}

\item{keep_core_cols}{If \code{TRUE}, always include \code{estudio}, \code{fecha}, \code{genero},
and \code{edad} when \code{cols} is supplied.}

\item{collect}{If \code{TRUE}, return a tibble in memory. If \code{FALSE}, return a lazy
Arrow/dplyr object.}
}
\value{
A tibble or a lazy Arrow query.
}
\description{
Uses Arrow and dplyr to push filters and column selection down before data is
collected into memory.
}
\examples{
\dontrun{
cis_read(fecha_min = "2023-01-01")

cis_read(
  fecha_min = "2020-01-01",
  fecha_max = "2024-12-31",
  cols = c("estudio", "fecha", "genero", "edad", "idv", "recuerdo")
)

cis_read(fecha_min = "2020-01-01", collect = FALSE) |>
  dplyr::count(estudio) |>
  dplyr::collect()
}
}
